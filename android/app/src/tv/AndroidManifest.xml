<?xml version="1.0" encoding="utf-8"?>
<!-- What separates the television build from the phone one. Everything else is
     merged in from the main manifest: leanback is asked for outright here, and a
     touchscreen is declared unnecessary, so the launcher on a television will
     show the app and a phone store will not offer it. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.software.leanback"
        android:required="true"
        tools:replace="android:required" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"
        tools:replace="android:required" />
</manifest>
